[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "enable-edge-engine"
version = "0.1.0"
description = "プロフェッショナル向け競馬データ分析プラットフォーム"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
pythonpath = ["."]
